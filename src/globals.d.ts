declare global {
  /**
   * This formats a date valie to a human-readable format
   * @param date The date to format
   * @returns A string with the date formatted
   */
  function formatDate(date: Date): string
}

export {}